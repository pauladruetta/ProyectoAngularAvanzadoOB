import { NombreCompletoPipe } from './nombre-completo.pipe';

describe('NombreCompeltoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreCompletoPipe();
    expect(pipe).toBeTruthy();
  });
});
